# 全文优化与重构方案

## 1. 审查依据与总体判断

本轮审查依据为 `C:\Users\AgiUser\Desktop\代码\7.2\competition\render_final_docx\final_readable_draft.md`、`C:\Users\AgiUser\Desktop\代码\7.2\competition\06_stage_revised_draft.md`、`C:\Users\AgiUser\Desktop\代码\7.2\competition\06_review_report.md`，并读取了当前比赛文档 docx 的对应内容。docx 当前包含 144 个段落和 7 个表格，章节内容与 `final_readable_draft.md` 基本一致，Mermaid 图在 docx 中已替换为图片占位。

当前文档技术事实整体可控，已经区分 RV1126B 端侧 AI、HTTP-FLV 与 RTSP relay、MQTT 真实发布与 log fallback、COCO class-aware 与 BottleBoxesOnly。主要问题不在事实错误，而在表达重心：摘要和第一部分仍偏“代码模块说明”，第二部分信息足够但略冗长，第三部分虽然未编造实测结果，但部分“已完成”“工程成果”语气还需要更保守。下一轮应围绕“比赛作品可读性”和“证据链完整性”重构，而不是继续堆源码细节。

## 2. 逐节检查与重构建议

### 2.1 作品名称

1. 当前主要问题：名称准确，但当前 docx 首页出现两次标题，阅读上略显重复。
2. 是否需要重写：不需要重写，只需要排版时去重。
3. 哪些内容保留：保留“基于 RV1126B 的端侧视觉健康守护系统”。
4. 哪些内容删除：删除重复标题或重复封面标题。
5. 哪些内容移动到其他章节：无。
6. 是否需要图或表：不需要。
7. 推荐修改后的写作方向：标题保持唯一、醒目，后续摘要直接承接“端侧视觉健康守护系统”的定位。

### 2.2 摘要

1. 当前主要问题：摘要技术事实较完整，但像代码链路说明，出现 CameraDevice、ImageProcessor、GestureModel 等模块名，比赛作品摘要的场景价值、作品目标和工程闭环表达不够凝练；ST7789 提醒状态虽然写了待照片验证，但占用摘要篇幅过多。
2. 是否需要重写：建议重写。
3. 哪些内容保留：保留桌面学习/办公场景、RV1126B 端侧 AI、本地感知与判断、多模型协同、手势启停、坐姿提醒、饮水提醒、ST7789、HTTP-FLV/RTSP relay、MQTT、非云端识别。
4. 哪些内容删除：删除大量类名和函数式模块名；删除 BottleBoxesOnly 的 `class_id=-1` 细节；删除过细的输出链路说明。
5. 哪些内容移动到其他章节：BottleBoxesOnly、COCO class-aware、模型路径、ST7789 显示优先级移动到 2.3；具体端口和 topic 移动到 1.4 或 2.3。
6. 是否需要图或表：摘要不需要图表。
7. 推荐修改后的写作方向：按“背景问题 -> 作品目标 -> 系统组成 -> 核心功能 -> 技术特点 -> 应用价值”重写，少写代码名，多写作品解决的问题和端侧闭环。摘要应像比赛作品摘要，而不是源码架构清单。

### 2.3 1.1 功能与特性

1. 当前主要问题：功能列得完整，但 `/dev/video23`、640×480、NV12 等配置细节在功能概述中过早出现；“本地反馈”对 ST7789 坐姿/饮水提醒仍偏肯定，虽然没有直接写实测成功。
2. 是否需要重写：建议局部重写。
3. 哪些内容保留：保留摄像头采集、手势 Start/Stop/Confirm/Rock、坐姿监测、饮水提醒、本地显示、HTTP-FLV/RTSP、MQTT 状态同步。
4. 哪些内容删除：删除设备节点和格式细节，或改为一句“配置参数见 1.4”；删除“显示坐姿和饮水提醒状态”的确定性结果口吻。
5. 哪些内容移动到其他章节：设备节点、分辨率、模型输入尺寸移动到 1.4；ST7789 状态优先级移动到 2.3.2.8。
6. 是否需要图或表：需要“系统功能示意图”，展示输入、识别、判断、反馈四层。
7. 推荐修改后的写作方向：用面向用户和评委的功能语言写“系统能做什么”，技术参数只保留必要点。

### 2.4 1.2 应用领域

1. 当前主要问题：三类场景清楚，医疗诊断边界明确；但语言略平，缺少每个场景的“为什么适合本作品”的差异。
2. 是否需要重写：小幅润色即可。
3. 哪些内容保留：保留学生自习、办公桌面、嵌入式 AI 教学竞赛展示三类场景；保留“不作为医疗诊断设备”。
4. 哪些内容删除：删除过多技术模块枚举，避免把教学场景写成源码清单。
5. 哪些内容移动到其他章节：RKNN、状态机、视频推流等工程细节移动到 1.3 或第二部分。
6. 是否需要图或表：可加入桌面使用场景图或实物摆放照片，占位到第三部分补图。
7. 推荐修改后的写作方向：按“自习场景强调非接触和提醒，办公场景强调本地隐私和状态同步，教学竞赛场景强调端侧工程链路”展开。

### 2.5 1.3 主要技术特点

1. 当前主要问题：内容准确，但段落较长，多个技术点挤在一段里；“技术特点”与第二部分软件说明有重复。
2. 是否需要重写：建议重构为 4 到 5 个短点。
3. 哪些内容保留：保留 RV1126B、RKNN/NPU、RGA/OpenCV、多模型协同、状态机防抖、ST7789/LVGL、MPP、HTTP-FLV、RTSP relay、MQTT。
4. 哪些内容删除：删除代码模块调度细节和长链路堆叠。
5. 哪些内容移动到其他章节：具体模块文件、线程、阈值和输出格式移动到 2.3。
6. 是否需要图或表：可配“软件架构图”，但正式图主要放在第二部分。
7. 推荐修改后的写作方向：采用“端侧推理特点、图像预处理特点、多模型融合特点、多端反馈特点、工程可调试特点”的结构。

### 2.6 1.4 主要性能指标

1. 当前主要问题：表格事实稳妥，但标题叫“性能指标”，实际多为配置参数；未实测性能都留空是正确的，但应避免让评委误解为性能结果缺失。
2. 是否需要重写：不需要大改，建议改为“主要配置与待测性能指标”或在表前说明。
3. 哪些内容保留：保留摄像头 640×480 NV12、模型输入尺寸、ST7789 240×240、HTTP-FLV 8080、RTSP relay 8554、MQTT topics、饮水提醒 30 分钟。
4. 哪些内容删除：删除可能被理解为实测结论的语气。
5. 哪些内容移动到其他章节：平均帧率、延迟、功耗、准确率等仍留到第三部分实测表，不进入第一部分。
6. 是否需要图或表：必须保留表格。
7. 推荐修改后的写作方向：明确“本表为当前实现配置，实测性能参数将在第三部分补充”，不写 FPS、延迟、准确率、功耗。

### 2.7 1.5 主要创新点

1. 当前主要问题：方向合理，但“健康提醒闭环”“多端反馈”略泛；需要更贴合本项目的端侧多模型、状态机和展示链路。
2. 是否需要重写：建议轻度重写。
3. 哪些内容保留：保留端侧多模型协同、非接触手势、视觉饮水判断与定时提醒融合、多端反馈、模块化部署。
4. 哪些内容删除：删除容易显得空泛的“便于部署和调试”类结尾，改为更具体的“资源受限平台链路整合”。
5. 哪些内容移动到其他章节：详细技术解释移动到 1.3 和 2.3。
6. 是否需要图或表：不需要图表，逐点即可。
7. 推荐修改后的写作方向：每点一句话，写成“创新点 = 项目组合方式或工程机制”，避免“首创”“领先”等夸张表述。

### 2.8 1.6 设计流程

1. 当前主要问题：流程完整，但像普通开发步骤，缺少“需求如何驱动系统模块”的表达。
2. 是否需要重写：建议小幅重写。
3. 哪些内容保留：保留需求分析、平台选型、采集链路、RKNN 模型部署、预处理与坐标映射、状态机、显示/推流/MQTT 联调、实机测试。
4. 哪些内容删除：删除“实机测试与参数优化”中可能暗示已经完成充分测试的语气，可改为“实机验证与参数调整”。
5. 哪些内容移动到其他章节：具体测试结果移动到第三部分。
6. 是否需要图或表：需要“设计流程图”。
7. 推荐修改后的写作方向：用流程箭头加一句解释，突出从场景需求到端侧闭环的设计逻辑。

### 2.9 2.1 整体介绍

1. 当前主要问题：技术链路准确，但文字偏长；Markdown 中有 Mermaid，docx 中只有图片占位，后续必须补真实系统整体框图图片。
2. 是否需要重写：建议压缩重写。
3. 哪些内容保留：保留数据流“摄像头 -> 预处理 -> 三模型 -> 状态融合 -> 显示/视频/MQTT”，保留控制流 Idle/Running 逻辑。
4. 哪些内容删除：删除重复强调“不是单一模型演示”的句子，只保留一次。
5. 哪些内容移动到其他章节：具体类名细节移动到 2.3；硬件细节移动到 2.2。
6. 是否需要图或表：必须新增系统整体框图，正式 Word 中不能只放占位。
7. 推荐修改后的写作方向：第一段写系统组成，第二段写数据流，第三段写控制流，配一张图2-1。

### 2.10 2.2 硬件系统介绍

1. 当前主要问题：硬件事实清楚，已避免编造 PCB/CAD；但 2.2.1、2.2.2、2.2.3 之间仍有重复，ST7789 状态显示描述应继续保守。
2. 是否需要重写：建议局部重构。
3. 哪些内容保留：保留 RV1126B、摄像头、ST7789、网络、上位机；保留桌面摆放式结构；保留模块连接关系而非自研 PCB。
4. 哪些内容删除：删除重复的“HTTP-FLV、RTSP relay、MQTT”描述；删除“显示坐姿和饮水提醒页面”的结果口吻。
5. 哪些内容移动到其他章节：ST7789 页面状态和显示优先级移动到 2.3.2.8；实物照片和接线成果移动到第三部分。
6. 是否需要图或表：保留硬件组成表、电路/连接模块说明表；需要硬件连接图或实物连接照片。
7. 推荐修改后的写作方向：硬件部分只讲“有哪些硬件、如何连接、承担什么作用”，不把软件功能和待验证显示效果写得过满。

### 2.11 2.3 软件系统介绍

1. 当前主要问题：技术细节足够，但 2.3.2 各模块偏长，像源码说明书；有些阈值、类名、文件路径过密，影响比赛文档可读性。
2. 是否需要重写：建议重点重构。
3. 哪些内容保留：保留 App 主控、CameraDevice、ImageProcessor、GestureModel、PoseModel、CupModel、DrinkDetector、DisplayDevice、VideoPipeline/WebStreamer、RTSP relay、MqttClient、single_image_ai_debug 的模块划分。
4. 哪些内容删除：删除过多源码文件路径重复；删除无需放正文的日志名、临时输出文件路径和过细函数名；删除“当前代码已按 [1,5,8400] 验证”式容易误读的肯定语。
5. 哪些内容移动到其他章节：关键配置参数移动到 1.4；上板验证、日志截图、工具输出移动到第三部分。
6. 是否需要图或表：必须保留软件模块职责表；必须新增软件架构图、主程序运行流程图、手势状态机图、饮水提醒流程图。
7. 推荐修改后的写作方向：先用软件架构图和模块职责表建立全局，再用短段落说明关键模块。每个模块按“输入 -> 处理 -> 输出 -> 真实性边界”写，避免变成代码流水账。

### 2.12 3.1 整体介绍

1. 当前主要问题：当前写法比材料清单更可读，但“工程已经具备桌面摆放式演示形态”需要实物照片支撑，语气略偏成果化。
2. 是否需要重写：建议重写为更保守的可读占位版。
3. 哪些内容保留：保留系统链路已完成设计，保留待插入系统整体正面、斜 45°、运行状态照片。
4. 哪些内容删除：删除没有照片前的“已经具备演示形态”强表述，或改为“本节将结合照片说明”。
5. 哪些内容移动到其他章节：具体软件链路移动到 2.1；功能验证移动到 3.3。
6. 是否需要图或表：需要系统整体照片和运行状态照片。
7. 推荐修改后的写作方向：写成“本节用于展示系统整体搭建情况，待补照片作为证据”，不要提前给出完成结论。

### 2.13 3.2 工程成果

1. 当前主要问题：机械、电路、软件成果结构完整，但“成果”二字下的内容仍多为待补证据；软件成果中 ST7789、RTSP、MQTT 都已采用保守写法，方向正确。
2. 是否需要重写：建议在补材料前小幅重写，在补材料后再正式重写。
3. 哪些内容保留：保留桌面摆放式结构、模块连接成果、主程序运行、HTTP-FLV、RTSP relay、MQTT、ST7789 状态映射、单图调试工具。
4. 哪些内容删除：删除未拍照前的“朝向用户”“用于展示”等实物确定性语气，改为“照片需体现”；删除任何暗示 VLC 已播放成功、MQTT 已发布成功的句子。
5. 哪些内容移动到其他章节：技术原理移动到第二部分；测试结论移动到 3.3。
6. 是否需要图或表：需要实物照片、接线照片、ST7789 状态照片、VLC 截图、MQTT 截图、主程序日志截图。
7. 推荐修改后的写作方向：按“机械成果以实物照片证明，电路成果以连接照片证明，软件成果以运行截图证明”写，所有结果性句子必须带证据占位。

### 2.14 3.3 特性成果

1. 当前主要问题：功能验证表和性能参数表方向正确；但当前表格较长，正式稿可以按“配置值”和“实测值”分层，避免第三部分显得像空表。
2. 是否需要重写：材料补齐后需要重点重写。
3. 哪些内容保留：保留功能验证表、性能参数表；保留待填写、待判定；保留未实测性能不可写原则。
4. 哪些内容删除：删除无法获得证据的测试项，或将其明确标为“未完成验证”；删除“预期结果”中可能误导为已经达到的表述。
5. 哪些内容移动到其他章节：配置参数移动到 1.4；验证方法细节可放附录或实验记录，不宜全部塞进正文。
6. 是否需要图或表：必须保留功能验证表和性能参数表；需要 ST7789 坏姿势/饮水提醒照片、BottleBoxesOnly 日志截图。
7. 推荐修改后的写作方向：先写已采集证据编号，再按功能逐项给出“验证方法、证据、结论”。没有证据的项保持待补，不写完成。

### 2.15 4.1 可扩展之处

1. 当前主要问题：方向全面，但一句话过长，扩展方向之间略拥挤。
2. 是否需要重写：建议改为分点。
3. 哪些内容保留：保留模型能力扩展、硬件交互扩展、MQTT 数据统计、结构产品化、端侧性能优化、多场景扩展。
4. 哪些内容删除：删除“服务器”可能被误解为云端 AI 识别的表达，保留为“上位机或数据管理端统计”。
5. 哪些内容移动到其他章节：无。
6. 是否需要图或表：不需要。
7. 推荐修改后的写作方向：写 4 到 6 个短点，每点一句，强调后续扩展不改变端侧推理主线。

### 2.16 4.2 心得体会

1. 当前主要问题：工程细节真实，但内容较像开发复盘，比赛文档里的“心得体会”可以更凝练；部分内容与第二部分重复，如 HTTP-FLV/RTSP、MQTT fallback。
2. 是否需要重写：建议中度润色。
3. 哪些内容保留：保留端侧 AI 不只是跑模型、模型输出解析、嵌入式资源占用、端口占用、MQTT fallback、ST7789 显示优先级、状态机防抖等工程体会。
4. 哪些内容删除：删除重复技术解释；删除过多变量名和路径；删除“开发过程中可以明显看到”这类口语化开头。
5. 哪些内容移动到其他章节：具体事实和配置移动到 2.3；第三部分证据要求移动到 3.3。
6. 是否需要图或表：不需要。
7. 推荐修改后的写作方向：写成 4 段：系统集成认识、模型部署认识、嵌入式联调认识、状态机与用户体验认识。语气正式，不写“我/我们”。

### 2.17 参考文献

1. 当前主要问题：方向覆盖较全，数量合适；多数条目仍是“待核对”，正式提交前必须补版本、访问日期或出版信息。
2. 是否需要重写：不需要重写，需要人工核对补全。
3. 哪些内容保留：保留 RKNN、RV1126/RV1109、V4L2、OpenCV、LVGL、GStreamer、MQTT、H.264、YOLO、姿态估计相关文献。
4. 哪些内容删除：删除无法确认来源的条目；不要新增博客作为核心参考。
5. 哪些内容移动到其他章节：无。
6. 是否需要图或表：不需要。
7. 推荐修改后的写作方向：保持 20 篇以内，采用统一 GB/T 7714 风格；不编 DOI、页码和访问日期，不确定则继续标注“待核对”。

## 3. 重点真实性与风格检查结论

1. 摘要目前偏代码流水账，需要改成比赛作品摘要，减少类名和配置细节。
2. 第一部分存在少量代码细节前置问题，应把设备节点、模型路径和输出格式集中到 1.4 或第二部分。
3. 第二部分技术细节足够，但 2.3 需要压缩冗余，避免像源码说明书。
4. 第三部分当前没有编造 FPS、延迟、准确率、功耗和稳定运行时长，方向正确。
5. ST7789 坏姿势、饮水提醒、DrinkOk 不能写成已实机显示成功，只能写代码侧状态映射存在，实物显示效果待照片验证。
6. 未发现把 BottleBoxesOnly 写成 `class_id=0` 的问题，当前写法是 `class_id=-1`，但摘要不应放这个细节。
7. HTTP-FLV 与 RTSP relay 当前区分清楚，后续图中也要画成“主程序 HTTP-FLV -> 独立 relay -> RTSP”。
8. MQTT 真实发布与 log fallback 当前区分清楚，第三部分必须以订阅窗口截图证明真实发布。
9. 医疗诊断表述目前控制得当，应继续保留“不作为医疗诊断设备”。

## 4. 推荐的重写顺序

1. 先重写摘要：确定全文叙事主线，避免后文继续像代码说明。
2. 再重构第一部分：把概述层的代码细节下沉，只保留作品功能、应用、特点和配置表。
3. 接着重构第二部分 2.3：压缩模块说明，配齐软件架构图、主流程图、手势状态机图、饮水提醒流程图。
4. 然后调整第三部分占位：把“成果”语气改成“证据待补”的正式占位版。
5. 最后润色第四部分和参考文献：统一风格、补齐参考文献信息。

## 5. 必须新增的流程图清单

1. 系统整体框图：展示摄像头、RV1126B、三模型、状态融合、ST7789、HTTP-FLV、RTSP relay、MQTT、上位机。
2. 软件架构图：展示 App 主控、采集线程、AI 线程、编码线程、显示线程、MQTT 线程和各模块关系。
3. 主程序运行流程图：展示启动、配置加载、硬件初始化、模型加载、循环采集、推理、状态融合、输出。
4. 手势状态机图：展示手势识别、阈值判断、稳定计数、冷却、释放锁、Start/Stop/Confirm/Rock 触发。
5. 饮水提醒流程图：展示 Pose/Cup 输入、头部点选择、杯/瓶距离归一化、连续命中、DrinkDetected、NeedRemind、定时提醒、Confirm 确认。
6. ST7789 显示优先级图：建议新增，展示手势临时反馈、Standby/SleepFace、DrinkOk、DrinkRemind、BadPosture、Normal 的优先级，专门解释为什么待机可能压过坏姿势显示。

## 6. 当前最应该先重写的 3 个部分

1. 摘要：目前最像代码流水账，影响评委第一印象。
2. 2.3 软件系统介绍：信息密度高但冗余，需要从源码说明重构为系统设计说明。
3. 第三部分 3.1 到 3.3：必须继续保持不编造成果，同时让占位稿更像正式文档待补版。

## 7. 暂时不能写死的成果和性能参数

1. 不能写已实测 FPS、平均帧率、端到端延迟、单次推理耗时、CPU 占用、内存占用、功耗、连续运行时长。
2. 不能写 ST7789 坏姿势提醒、饮水提醒、DrinkOk 已经实机显示成功，除非补充照片和对应日志。
3. 不能写 VLC RTSP 已成功播放，除非补充 RTSP relay 日志和 VLC 截图。
4. 不能写 MQTT 已真实发布成功，除非 `mosquitto_sub` 订阅窗口收到 `rv1126b/status`、`rv1126b/event` 或 `rv1126b/telemetry`。
5. 不能写 BottleBoxesOnly `[1,5,8400]` 已上板稳定验证，除非补充板端日志或单图调试截图。
6. 不能写自研 PCB、完整机械外壳、CAD、机加工成果，除非后续补充真实资料。
7. 不能写医疗诊断、健康疗效、准确率提升或长期健康改善效果。
8. 不能把当前主程序写成直接输出 RTSP；只能写主程序输出 HTTP-FLV，RTSP relay 单独转换。
9. 不能把 MQTT log fallback 写成真实网络发布。
10. 不能把 BottleBoxesOnly 写成 `class_id=0` 或 COCO 当前唯一模式。

## 8. 输出自查

| 检查项 | 结果 |
|---|---|
| 是否只生成全文优化与重构方案 | 是 |
| 是否修改 docx | 否 |
| 是否写正式正文 | 否 |
| 是否生成新 Word | 否 |
| 是否逐节检查指定章节 | 是 |
| 是否检查摘要代码流水账问题 | 是 |
| 是否检查第一部分代码细节问题 | 是 |
| 是否检查第二部分技术冗余问题 | 是 |
| 是否检查第三部分成果编造风险 | 是 |
| 是否列出缺失流程图 | 是 |
| 是否写入未实测性能指标 | 否 |
| 是否写医疗诊断效果 | 否 |
| 是否混淆 HTTP-FLV 与 RTSP relay | 否 |
| 是否混淆 MQTT fallback 与真实发布 | 否 |
| 是否把 BottleBoxesOnly 写成 class_id=0 | 否 |
| 是否修改代码 | 否 |
